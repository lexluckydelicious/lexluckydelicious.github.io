part one

 git checkout -b NAME_OF_BRANCH

ff merge moves the branch linearly from current to target, recursive merges two common
ancestors into a new virtual ancestor

 git checkout NAME_OF_BRANCH

 git branch -d NAME_OF_BRANCH

merge conflicts happen when you merge branches witn conflicting commits, and are resolved via different strategies

part two

mkdir learn_branching
cd learn_branching
git init
touch first.txt
git add first.txt
git commit -m "adding first.txt"
git checkout -b feature_branch
git checkout feature_branch
touch first.txt
git add first.txt
git commit -m "adding first to feature_branch"
git checkout main
git merge feature_branch

